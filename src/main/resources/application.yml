spring:
  application:
    name: test-moon
  web:
    resources:
      static-locations: classpath:/front/,classpath:/atlas/,classpath:/tiles/,classpath:/sphere/,classpath:/user/

server:
  port: 8080
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

